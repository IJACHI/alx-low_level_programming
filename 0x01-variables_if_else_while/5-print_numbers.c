#include <stdio.h>

/**
 * main - This Prints numbers from 0 to 9.
 *
 * Return: This Always returns 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
